# AGENTS.md

## 开发准则

### 1. 先理解，再修改
- 修改前先阅读相关代码和目录结构。
- 优先搜索已有实现，禁止重复造轮子。
- 不确定的 API、类型、项目约定必须先确认，不允许猜测。

### 2. 单一职责
- 一个文件、类、函数只负责一类明确职责。
- 业务逻辑、数据处理、UI、工具函数按职责分离。
- 避免巨型文件、巨型函数和巨型组件。

### 3. 简单优先
- 优先最简单、清晰、可维护的实现。
- 不过度设计，不过早抽象。
- 不为了“未来可能使用”增加抽象层。
- 能复用现有代码就不要重新实现。

### 4. 最小修改
- 只修改当前任务需要的代码。
- 不修改无关文件。
- 不进行无关重构、依赖升级或格式化。
- 保护用户已有代码和修改。

### 5. 目录与文件
- 目录按职责组织。
- 文件名必须表达实际职责。
- 避免 `common`、`helper`、`misc`、`temp`、`utils2` 等无明确含义的文件。
- 文件过大时按职责拆分，而不是机械按行数拆分。

### 6. 命名
- 使用清晰、完整、有实际含义的名称。
- 函数使用动词表达行为：`getUser`、`createUser`、`loadConfig`。
- 布尔值使用 `is`、`has`、`can`、`should`。
- 常量使用 `UPPER_SNAKE_CASE`。
- 遵循项目已有命名风格。

### 7. TypeScript
- 优先保持严格类型安全。
- 禁止无理由使用 `any`。
- 不通过 `as any`、`@ts-ignore` 等方式逃避类型检查。
- 公共 API、核心函数和复杂数据结构必须有明确类型。

### 8. 注释
- 注释使用中文。
- 注释重点解释“为什么”，而不是重复代码行为。
- 复杂逻辑、生命周期、并发、性能优化必须添加必要说明。
- 不添加无意义的逐行注释。
- 对外暴露的核心类、函数、类型使用简洁中文 JSDoc。

### 9. 错误与资源
- 不允许静默吞掉异常。
- 错误必须合理处理、记录或向上层传递。
- 创建的 Timer、Listener、Subscription、WebSocket、Worker 等资源必须考虑释放。
- 日志禁止输出 Token、密码、Cookie、Secret 等敏感信息。

### 10. 依赖
- 新增依赖前先确认现有项目没有相同能力。
- 简单功能不要引入大型依赖。
- 遵循项目现有依赖和技术栈。

### 11. 验证
完成修改后，根据项目实际情况执行：

1. TypeCheck
2. Lint
3. Test
4. Build

发现问题必须修复后再交付。

### 12. 核心原则

> 单一职责、简单优先、最小修改、复用优先、类型安全、先理解后修改、先验证后交付。